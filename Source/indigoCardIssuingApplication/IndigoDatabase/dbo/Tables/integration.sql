CREATE TABLE [dbo].[integration] (
    [integration_id]   INT           NOT NULL,
    [integration_name] VARCHAR (150) NOT NULL,
    CONSTRAINT [PK_integration] PRIMARY KEY CLUSTERED ([integration_id] ASC)
);

